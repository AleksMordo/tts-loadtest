# Сгенерированный protoc код использует плоский `import tts_pb2` —
# добавляем каталог пакета в sys.path, чтобы работал импорт из loadgen.proto.
import os
import sys

sys.path.insert(0, os.path.dirname(__file__))
